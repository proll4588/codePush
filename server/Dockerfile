# Используем официальный образ Node.js. Alpine - легковесная версия Linux.
FROM node:20-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /usr/src/app

# Копируем package.json и package-lock.json для установки зависимостей
# Это кэшируется Docker, и npm install не будет запускаться, если файлы не изменились
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальные файлы приложения в рабочую директорию
COPY . .

# Открываем порт, на котором работает приложение
EXPOSE 3000

# Команда для запуска приложения при старте контейнера
CMD [ "node", "index.js" ]
